# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Bun
bun.lockb
.bun

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
.fleet
.idea
.vscode
.claude
.qodo
tmpclaude-*

# Local env files
.env
.env.*
!.env.example

# k8s manifests with real secrets. The .example template stays in git;
# the populated copies (Mongo URI, JWT secrets, GitHub PAT) do not.
k8s/secret.yaml
k8s/ci/runner-token-secret.yaml

# Production Docker files (keep dev versions only)
docker-compose.yml
docker-compose.prod.yml

# Docker volumes and temp files
.dockerignore
.claude